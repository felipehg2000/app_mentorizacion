import './bootstrap';
import 'laravel-datatables-vite';
import 'vue';

/*const app = new Vue({
    el: '#app',
    mounted(){
        window.Echo.channel('sync_chat')
        .listen('NewMessageEvent', (e) => {
            console.log('Mensaje en tiempo real')
        });
    }
});
*/
